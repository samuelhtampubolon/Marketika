// Emits packages/app/src/locale/generated/{id,en}.ts from the spec.
// P00 stub: emission begins in phase P15 (locale system).
export function emitLocale(_spec: Record<string, unknown>): void {
  console.log("codegen: emitLocale is a P00 stub (activation: P15)");
}
