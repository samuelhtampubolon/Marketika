{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the local Marketika window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
